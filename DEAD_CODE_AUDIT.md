# تدقيق الكود الميت والمشكلات المحتملة — الصقري للاتصالات

تاريخ التدقيق: 2026-08-22 (على فرع fix/firestore-reads بعد جولات التنظيف السابقة)

> ## حالة التنفيذ (بعد جولة "طبق التوصيات")
> - ✅ **1. sw.js**: أُصلح — الإصدار v2 + network-first للصفحات/JS/CSS وcache-first للأيقونات فقط (commit d1a3b9a).
> - ✅ **أ. الملفات الأربعة + base.html**: حُذفت نهائيًا (commit eef3a3f).
> - ✅ **ج. includesAny + كعب updateLimitedDashboard + تكرار convertArabicToEnglishNumbers في search**: حُذفت (eef3a3f).
> - ✅ **د. كود العرض الميت في create_sale وmaintenance-jobs**: حُذف — وتبين عند التنفيذ أن المعرفات الخمسة كلها (بما فيها totalProfit/techCommission) غير موجودة في maintenance-jobs (eef3a3f).
> - ⚠️ **تصحيح هـ**: مفتاحا `accessory_categories` و`phone_types` **ليسا ميتين** — يكتبهما storage-manager عبر ثوابت `CONFIG.STORAGE_KEYS` في وضع عدم توفر Firebase (الفاحص الآلي فاته لأن المفتاح متغير لا نصًا). أُبقيا. المفتاح `lastAddedAccessory` أُصلح بكتابته في add_accessory بعد الحفظ (d4a7f67) فأصبح مسار الطباعة بدون قراءة Firestore يعمل.
> - ✅ **و. صنف fixed-top**: حُذف من CSS (eef3a3f).
> - ✅ **2. انحراف config.js**: حُسم بالحذف + ترسيخ `vatRate = 0.15` صراحةً في create_sale مع تعليق يوثق القرار (d4a7f67).
> - ⏳ **3. توحيد logout (12 تعريفًا) وتوحيد دوال الأرقام العربية (9 نسخ)**: مؤجل لجولة مستقلة كما أوصى التقرير — تعديل واسع يحتاج اختبار تدفقات الحفظ.
> - ⏳ **4. _redirects**: بقي كما هو عمدًا — تغيير سلوك الاستضافة بدون اختبار على Render مخاطرة؛ السلوك الحالي (Netlify-style rewrite) يقدّم الملفات الموجودة ولا يعيد كتابتها.


---

# خطة البحث المطبّقة (قابلة لإعادة الاستخدام)

1. **ملفات ميتة كاملة**: لكل ملف JS/CSS، grep في كل الصفحات + sw.js + manifest.json + _redirects + firebase.json — الملف الذي لا يشير إليه أحد وليس نقطة دخول = ميت.
2. **دوال ميتة داخل الملفات الحية**: سكربت Node يستخرج كل تصريحات الدوال (function/arrow) من السكربتات المضمّنة والملفات المحمّلة، ويقاطعها مع كل الاستخدامات (نداءات + مراجع نصية + onclick) في المشروع كله.
3. **مراجع مكسورة**: onclick/onchange لمعرفات دوال غير معرّفة، getElementById لمعرّفات غير موجودة في الصفحة ولا تُنشأ ديناميكيًا، href لأهداف غير موجودة.
4. **حالة localStorage**: كل مفتاح يُكتب ولا يُقرأ، أو يُقرأ ولا يكتبه أحد.
5. **التكوينات والأصول**: sw.js (استراتيجية الكاش + إصداره)، manifest.json (الأيقونات)، _redirects، icons/.
6. **CSS**: أصناف معرّفة لا تظهر في أي HTML/JS.
7. **تكرارات خطرة**: تعريف الدالة نفسها في أكثر من مكان/صفحة (ظلال وحسم متأخر).
8. **تحقق يدوي بعد كل مرشح آلي** (لا اعتماد على الأسماء وحدها) — كل بند أدناه مُثبت بسطر دليل.

---

# أولًا: أكواد ميتة مؤكدة — آمنة للحذف

## أ. أربعة ملفات JS كاملة لا تحمّلها أي صفحة (698 سطرًا)

| الملف | الأسطر | الدليل |
|---|---|---|
| js/arabic-font.js | 115 | grep في كل *.html + sw.js + manifest = صفر إشارة |
| js/config.js | 150 | صفر إشارة — **انظر ملاحظة الانحراف في القسم الثاني** |
| js/firebase-config.js | 46 | صفر إشارة (الصفحات كلها تستخدم firebase-config-cdn.js) |
| js/utils.js | 387 | صفر إشارة (كان مستهلكه الوحيد sales-manager.js المحذوف سابقًا) |

## ب. صفحة قالب ميتة
- **base.html** — لا يشير إليها أي ملف (grep = صفر)، وفيها رابط لملف غير موجود (`logout.html`، السطر 115) و`isLoggedIn = true` مضمّنة (السطر 148). قالب تجريبي لم يُستخدم قط.

## ج. دوال ميتة داخل الصفحات الحية
| الدالة | الموقع | الدليل |
|---|---|---|
| `includesAny` | search.html:199 | معرّفة ولا يوجد أي نداء لها في المشروع |
| `updateLimitedDashboard` | limited_dashboard.html:226 | كعب فارغ `return;` لم يعد يستدعيه أحد (استدعاؤه أُزيل في تبسيط المرحلة ب) |

## د. كود عرض ميت (محصّن بـ if لكن أهدافه غير موجودة في HTML)
- create_sale.html:621-632 — إزالة عنصري `#cart_purchase_cost` و`#cart_profit` اللذين غير موجودين أصلًا في الصفحة (تعليق الكود نفسه: "إخفاء صفوف... غير مرئي").
- maintenance-jobs.html:555-565 و585-598 — تحديث `#techPercentDisplay` و`#shopProfit` و`#profitCalculation` بفحوص if-null لأن العناصر غير موجودة في DOM الصفحة.

## هـ. قراءات localStorage ميتة
- `lastAddedAccessory` — **تُقرأ** في print_accessory_barcode.html:159,234 ولا يكتبها أي ملف (add_accessory.html لا ينفّذ setItem لها) — مسار التسليم السريع معطوب منذ نشأته؛ الصفحة تسقط دائمًا إلى استعلام Firestore (يعمل بعد إصلاحنا، لكن القصد الأصلي ميت).
- `accessory_categories` — تُقرأ في fallback بـ create_sale.html ولا يكتبها أحد.
- `phone_types` — تُقرأ في fallback بـ edit_phone.html ولا يكتبها أحد.

## و. CSS
- صنف `fixed-top` واحد فقط غير مستخدم من أصل 30 في app-mobile.css (وهو أصلًا صنف Bootstrap مضاعف) — حذف اختياري بلا أثر.

---

# ثانيًا: أكواد قد تسبب مشاكل (ليست ميتة لكنها خطرة)

## 1. أخطرها: sw.js يقدّم نسخًا قديمة دائمًا (sw.js:6 + معالج fetch)
- استراتيجية **cache-first لكل GET** مع `CACHE_NAME = 'alsaqri-pwa-v1'` **لم يتغير إطلاقًا** — أي تحديث تنشره لن يراه المستخدمون الذين زاروا الموقع من قبل (تبقى صفحاتهم وJS القديم في الكاش للأبد حتى يغيَّر اسم الإصدار أو يمسحوا الكاش يدويًا).
- هذا يعني أن كل إصلاحات هذا الفرع قد لا تصل للمستخدمين الحاليين بعد النشر. **أول إصلاح مستقبلي مقترح: رفع رقم الإصدار مع كل نشر أو التحويل إلى network-first للـ HTML/JS.**

## 2. انحراف config.js (الملف الميت له ظل حي)
- create_sale.html:906 وdashboard يستخدمان `CONFIG` بـ `typeof CONFIG !== 'undefined'` مع قيم احتياطية مضمّنة (0.15 للضريبة) — وبما أن config.js لا يُحمّل أبدًا فالاحتياطي هو المعمول به دائمًا وصامتًا. أي تغيير مستقبلي في config.js لن ينعكس — إما حذف الملف والاعتراف بالقيم المضمّنة، أو تحميله فعلًا.

## 3. تكرار تعريف الدوال (خطر صيانة وظلال)
- `logout` معرّفة **12 مرة**: navigation.js + 11 صفحة، بسلوك مختلف (dashboard يمسح الكاش ويذهب لـ index.html، list_accessories يذهب لـ index.html، والباقي لـ login.html) — تعريف الصفحة يحجب تعريف navigation.js حسب ترتيب التحميل. يعمل اليوم، لكنه قنبلة صيانة.
- `convertArabicToEnglishNumbers` معرّفة **مرتين في نفس الملف** (search.html:171 و:205) — الثاني يحجب الأول بصمت.
- دوال الأرقام العربية (parseArabicNumber/convertArabicToEnglishNumbers/setupArabicNumberSupport) مكررة inline في 6 صفحات لا تحمّل js/arabic-numbers.js بينما 6 صفحات صيانة تحمّله — 9 نسخ من نفس المنطق في المشروع. أي إصلاح مستقبلي يجب أن يطبَّق 9 مرات وإلا حصل تناقض.

## 4. _redirects يعيد كتابة كل شيء إلى index.html
- `/* /index.html 200` — أي رابط خاطئ (حتى .js أو .html غير موجود) يعيد صفحة index بحالة 200 بدل 404 — يخفي الأخطاء ويجعل تشخيص الملفات المفقودة صعبًا.

---

# ثالثًا: ما فحصته وسليم (لا تلمسه)

- مفاتيح localStorage الأساسية متوازنة: `lastSale` (يُكتب create_sale:946 ويُقرأ view_sale:570)، `lastAddedPhone` (يُكتب في add_new:451 وadd_used:485 ويُقرأ print_barcode:185)، `current_user`، كاش Firestore، عدادات الباركود — كلها تُكتب وتُقرأ.
- أيقونات manifest (icon-192/512.png) موجودة ومشُورة بشكل سليم.
- روابط href الداخلية كلها تشير لملفات موجودة (عدا logout.html في base.html الميتة).
- لا يوجد onclick لدالة غير معرّفة في أي صفحة (النتيجة الوحيدة كانت onkeypress="if(...)" — تعبير شرطي سليم).
- CSS عمليًا نظيف (29/30 مستخدم).
- ملفا firebase-database-cdn.js وfirebase-storage-manager.js بعد جولة التنظيف الأخيرة: كل دوالهما المتبقية لها مستدعٍ حقيقي (فحص آلي + يدوي).

---

# التوصيات مرتبة بالأولوية

1. **إصلاح sw.js قبل نشر هذا الفرع** (أو على الأقل رفع v1→v2 مرة واحدة) — وإلا لن تصل أي من إصلاحات القراءات للمستخدمين الحاليين.
2. حذف البنود المؤكدة في القسم الأول (أ+ب+ج+د+هـ) — كلها بصفر أثر وظيفي، في commit منفصل.
3. قرار في config.js: حذف نهائي أو تحميل فعلي (الوضع الحالي أسوأ الاثنين).
4. لاحقًا: توحيد logout في navigation.js وحذف النسخ الصفحية، وتوحيد دوال الأرقام العربية في الملف المشترك — كلاهما إعادة هيكلة صغيرة تستحق جولة مستقلة مع اختبار.

الفرق عن جولات التنظيف السابقة: تلك كانت دوال بلا مستدعٍ داخل ملفات محمّلة؛ هذه الجولة غطّت ملفات كاملة، وحدود DOM، ومفاتيح تخزين، وتكوينات نشر/كاش — أي الطبقة التي لا يظهر أثرها في كود الصفحات مباشرة.
