# تدقيق عمليات Firestore والتحميلات غير المفيدة لواجهة المستخدم

تاريخ التدقيق: 2026-08-22
النطاق: جميع صفحات HTML + js/firebase-database-cdn.js + js/firebase-storage-manager.js + كل ملفات JS المحمّلة من الصفحات.
ملاحظة منهجية: كل استدعاء `storage.getPhones()/getAccessories()/getSales()` يقرأ **المجموعة كاملة** من Firestore ثم يفلتر في المتصفح، مع كاش localStorage لمدة دقيقتين (`firebase-database-cdn.js:41`). أي "قراءة كاملة" أدناه تعني عدد مستندات المجموعة (N للهواتف، A للأكسسوارات، S للمبيعات، J لأعمال الصيانة).

---

# ملخص

- **العمليات المؤكدة غير المفيدة للواجهة (من الكود فقط): 12** عملية (البنود 1–12 في جدول النتائج المؤكدة).
- **العمليات المكررة (نفس المجموعة تُقرأ مرتين+ في نفس رحلة المستخدم): 7** تسلسلات مكررة موثقة أدناه.
- **العمليات المحتملة التي تحتاج اختبارًا بصريًا: 5** (مذكورة في قسم "تحقق بصري مطلوب").
- **نتيجة جانبية:** 9 ملفات JS كاملة غير محمّلة من أي صفحة (كود ميت مؤكد بـ grep على كل ملفات HTML).

---

# النتائج المؤكدة

| # | الأولوية | الصفحة | الملف:السطر | العملية | البيانات المقروءة | لماذا لا تظهر فائدتها للمستخدم؟ | الدليل من الكود | تكلفة القراءة التقريبية | الإجراء المقترح |
|---|---|---|---|---|---|---|---|---|---|
| 1 | عالية | كل الصفحات الـ 15 التي تحمّل storage-manager (تحقق بـ grep: add_accessory, add_new_phone, add_used_phone, create_sale, dashboard, edit_accessory, edit_phone, inventory_summary, limited_dashboard, list_accessories_simple, list_sales, print_accessory_barcode, print_barcode, search, view_sale — ولا تشمل صفحات الصيانة) | js/firebase-storage-manager.js:41 → js/firebase-database-cdn.js:153-160 | `primePhoneCounterBase()` عند تحميل كل صفحة | مستند واحد `counters/phones` | لا تخدم هذه الصفحات أي عنصر ظاهر؛ العداد يلزم فقط عند حفظ هاتف جديد (زر في صفحات الإضافة) | الاستدعاء في constructor بدون شرط؛ لا استخدام لأي قيمة عائدة في هذه الصفحات | 1 قراءة × كل فتح صفحة (غير مغطاة بأي كاش) | تأجيل الاستدعاء إلى لحظة الضغط على "حفظ" في صفحات إضافة الهواتف |
| 2 | عالية | create_sale.html | create_sale.html:879 (داخل `updateAccessoryStock`) → `loadDataFromFirebase()` :285-292 | إعادة تحميل 4 مجموعات كاملة بعد إتمام البيع | phones + accessories + accessory_categories + sales (كاملة) | الاستدعاء يعيد بناء القوائم المنسدلة فنيًا، لكن الصفحة تنتقل إلى view_sale.html بعد 800ms من إغلاق تنبيه النجاح (`:955-956`) — لا تفاعل ممكن للمستخدم مع البيانات الجديدة، والصفحة التالية تعيد قراءة ما تحتاجه بنفسها | التسلسل: `completeSale` → `updateAccessoryStock` (ينتهي بـ `await loadDataFromFirebase()`) → `addSale` → alert → `setTimeout(redirect, 800)` | N + A + C + S قراءة بلا فائدة استخدام **لكل عملية بيع** | حذف الاستدعاء من `updateAccessoryStock` أو جعله شرطيًا بعدم الانتقال |
| 3 | عالية | inventory_summary.html | inventory_summary.html:310 | `getSales()` كاملة | مجموعة sales كاملة (كل الحقول بما فيها مصفوفات items) | تُستخدم حصريًا داخل `isPhoneSold()` (:282-294) لاستبعاد الهواتف المباعة؛ لا يُعرض أي بيانات مبيع في الصفحة | الاستدعاء الوحيد للنتيجة هو `isPhoneSold()` عند :320-323 | S قراءة لاستخراج مجموعة أرقام مباعة فقط | where-query على المبيعات أو علم `sold` على مستند الهاتف |
| 4 | عالية | search.html | search.html:278 (و:743) | `getSales()` كاملة عند كل بحث | مجموعة sales كاملة | تُستخدم فقط لحساب شارة "مباع/متاح" لكل صف هاتف (`isPhoneSold` :225-253) وترتيب النتائج | كل الاستخدامات موثقة عند :401 و:446 فقط | S قراءة لكل نقرة بحث (بعد انتهاء كاش الدقيقتين) | نفس البند 3 |
| 5 | عالية | view_sale.html | view_sale.html:674 و:1060 | `getPhones()` كاملة — مرتان | مجموعة phones كاملة | تُستخدم فقط لاسترجاع `serialNumber` لعناصر الهاتف الناقصة عبر `phones.find(...)` (:686 و:1073) — خلية جدول واحدة لكل عنصر | الفحص `.find` على المصفوفة كاملة ثم إهمال الباقي؛ تكرار القراءة عند الطباعة | 2 × N قراءة لعرض فاتورة واحدة + طباعتها | getDoc مباشر لكل عنصر (المعرّف معروف من الفاتورة) |
| 6 | عالية | maintenance.html | maintenance.html:279 و:303 | `getMaintenanceJobs()` كاملة — مرتان عند كل فتح | مجموعة maintenanceJobs كاملة ×2 | القراءة الأولى (:279) لبطاقتي إحصاء (عددان صحيحان فقط)، والثانية (:303) لجدول الأعمال المعلقة بعد التخلص من done/canceled في المتصفح | استدعاءان متطابقان بلا فلاتر في نفس DOMContentLoaded | 2 × J قراءة لكل فتح صفحة | استدعاء واحد مشترك، أو `{status:'pending'}` للجدول |
| 7 | عالية | maintenance-accounts.html | maintenance-accounts.html:511 و:516 → firebase-database-cdn.js:1143 و:1237 | `getRepSettlements` + `getTechSettlements` | مجموعة maintenanceJobs (done + نطاق التاريخ) **مرتين** | كل دالة تقرأ نفس مجموعة الأعمال المنجزة بشكل مستقل ثم تجمعها في المتصفح | الدالتان تستدعيان `getMaintenanceJobs({status:'done',...})` كلٌّ على حدة | 2 × J₍done₎ قراءة لكل فتح/فلترة | استدعاء واحد للأعمال + تجميع محلي للمندوبين والفنيين معًا (النمط المطبق فعلًا في maintenance-accounts-report.html:401 يثبت إمكانيته) |
| 8 | متوسطة | maintenance-jobs-report.html | maintenance-jobs-report.html:398-400 | `getReps()` + `getTechnicians()` كاملتين | مجموعتا reps وtechnicians كاملتين | الاستخدامان الوحيدان: حل `repId`/`techId` إلى اسم في شريط "الفلاتر المطبقة" (:494 `repsData.find(...)`) — يلزم فقط عند وجود المعامل في الرابط — وعددان في `console.log` (:430-431) لا يراهما المستخدم؛ بدون المعاملات تُجلب المجموعتان بلا أي أثر في الواجهة | grep لكل استخدامات `repsData`/`techniciansData`: :430-431 (console.log) و:494 (حل الاسم) فقط | R + T قراءات لعرض اسمين على الأكثر | جلب شرطي فقط عند وجود المعامل، أو getDoc للمعرّف الواحد |
| 9 | متوسطة | edit_phone.html | edit_phone.html:479 و:529 | `getPhoneTypes()` مرتين متتاليتين عند الفتح | مجموعة phone_types كاملة ×2 | `loadManufacturers` ثم `loadModels` مباشرة بعدها (عبر `loadPhoneForEdit` :298) يقرآن نفس المجموعة | الاستدعاءان في نفس تسلسل DOMContentLoaded (:651, :658) | 2 × PT قراءة (يخففها كاش الدقيقتين أحيانًا) | تمرير نتيجة الاستدعاء الأول بدل إعادة الجلب |
| 10 | متوسطة | edit_phone.html + add_new_phone.html + add_used_phone.html + print_barcode.html + print_accessory_barcode.html + list_accessories_simple.html | edit_phone.html:271؛ add_new_phone.html:401,543؛ add_used_phone.html:433,579؛ print_barcode.html:200-208؛ print_accessory_barcode.html:174-188؛ list_accessories_simple.html:389,491 | "قراءة مجموعة كاملة لإيجاد سجل واحد" | phones أو accessories كاملة | كل حالة تنتهي بـ `.find()` لسجل واحد ثم إهمال الباقي (ملء نموذج تعديل، طباعة باركود، فتح نافذة تعديل من الجدول) | أنماط `.find()` موثقة في الأسطر المذكورة؛ نمط الاستعلام المفلتر موجود أصلًا في firebase-database-cdn.js:191-195 | N أو A قراءة بدل 1 | استبدالها بـ `where`-query أو getDoc بالمعرّف |
| 11 | متوسطة | add_accessory.html | add_accessory.html:597 | `loadCategories()` بعد حفظ أكسسوار | مجموعة accessory_categories كاملة | الحفظ لا يغيّر الفئات إطلاقًا؛ إعادة الجلب بلا فائدة عرضية | `saveAccessoryToFirebase` لا يمس الفئات، ثم يعيد تحميلها | C قراءة مهدورة لكل حفظ أكسسوار | حذف الاستدعاء بعد الحفظ |
| 12 | منخفضة | dashboard.html | dashboard.html:717-718 | حساب `totalRevenue` و`totalProfitRatio` (ليست قراءة إضافية، لكن نتيجة قراءة المبيعات) | — | القيمتان محسوبتان ولا تُعرضان؛ المعروض في `#profit_ratio` هو `actualProfitRatio` (:725) | مقارنة :717-718 بـ :725 | صفر قراءات إضافية (حساب ميت فقط) | حذف السطرين |

### عمليات ميتة إضافية مؤكدة (لا تنفذ أصلًا لكنها تنفذ قراءات لو استُدعيت)

- **list_accessories_simple.html:539,577** — `exportData()` و`clearAllData()` لا يستدعيهما أي زر في الصفحة (grep: صفر `onclick`)، و`clearAllData` يقرأ كل المجموعات ثم يحذف مستندًا مستندًا.
- **edit_accessory.html:183-193,301** — الصفحة تعمل على كائن وهمي ثابت ("غطاء شفاف")، لا تقرأ أي أكسسوار من Firestore، وتستدعي `updateAccessory(payload)` **بدون معرّف** (كود مكسور). لا يوجد رابط يصلها بمعرّف.
- **firebase-database-cdn.js:993-1015** — `getSettlements()` غير مستدعاة من أي صفحة، وفيها خطأ حاسم: السطر 996 `let query = collection(...)` يحجب دالة `query` المستوردة، فأي استدعاء بفلاتر يرمي TypeError.
- **مستمعو onSnapshot** (`firebase-database-cdn.js:663-694` عبر onPhonesChange/onAccessoriesChange/onSalesChange): صفر استدعاءات في كل الصفحات والملفات المحمّلة — مستمعون بلا مستهلك، لكنهم لا يكلفون شيئًا لأنهم لا يُشتركون أصلًا.
- **ملفات JS ميتة بالكامل** (grep على كل HTML = صفر تحميل): `js/main.js`، `js/auth.js`، `js/nav-helper.js`، `js/storage.js`، `js/barcode.js`، `js/phone-manager.js`، `js/accessory-manager.js`، `js/sales-manager.js`، و`js/firebase-database.js` (النسخة غير-CDN؛ تعليقها الداخلي :642 يقر بذلك). لا تنتج قراءات حاليًا لكنها مخاطرة إن أُعيد تحميلها.
- **limited_dashboard.html:220-249** — بنية تهيئة كاملة لكن `updateLimitedDashboard` هي `return;` فارغ؛ الصفحة لا تعرض إحصاءات (القراءة الوحيدة الفعلية فيها هي بند 1: primePhoneCounterBase).
- **view_sale.html معامل `?print=1`** — روابط "طباعة" في list_sales.html:569 تمرر المعامل، لكن view_sale.html لا يقرأ من URL سوى `id` (:564-565) — معامل ميت (المستخدم يجب أن يضغط زر الطباعة يدويًا).

---

# عمليات مكررة

1. **maintenance.html** (نفس فتح الصفحة): `getMaintenanceJobs()` كاملة :279 (للإحصاءات) ← `getMaintenanceJobs()` كاملة :303 (للجدول). = قراءة maintenanceJobs مرتين.
2. **maintenance-accounts.html** (نفس الفتح/الفلترة): `getRepSettlements` :511 ← داخليًا `getMaintenanceJobs({status:'done',…})` (cdn:1143) ← `getTechSettlements` :516 ← داخليًا نفس الاستعلام (cdn:1237). = نفس مجموعة الأعمال المنجزة مرتين، تسلسليًا.
3. **رحلة إتمام بيع** (create_sale ← view_sale): `loadDataFromFirebase` (4 مجموعات، :879، مهدورة) ← `addSale` ← انتقال ← view_sale: `getSale` (:585، مستند واحد — سليم) + `getPhones` كاملة (:674). الهواتف والمبيعات تُقرآن مرتين فعليًا في الرحلة الواحدة.
4. **view_sale.html عرض + طباعة**: `getPhones()` كاملة :674 عند العرض ← `getPhones()` كاملة :1060 عند نقر الطباعة.
5. **view_sale.html إرجاع**: `getAccessories()` كاملة :1246 ← بعد الإرجاع يعاد `displaySaleData` (:1289) ← `getPhones()` كاملة مرة أخرى (:674).
6. **search.html بعد حذف**: حذف أكسسوار ← submit اصطناعي (:684) ← `getPhones+getAccessories+getSales` (:276-278)؛ حذف هاتف ← `loadAllData()` (:913 → :741-743، نفس المجموعات الثلاث). = عملية حذف واحدة تكلف 3 قراءات كاملة + كتابة.
7. **edit_phone.html عند الفتح**: `getPhoneTypes()` :479 ← `getPhoneTypes()` :529 (loadModels).

ملاحظة: كاش الدقيقتين في firebase-database-cdn.js قد يخفي بعض هذه التكرارات خلال نفس الجلسة القصيرة، لكن التكلفة تعود كاملة بعد انتهاء صلاحيته، والكاش يُمسح تلقائيًا بعد كل كتابة (`_cacheClear`) — أي إن حذف المستخدم عنصرًا ثم بحث، تُقرأ المجموعات الثلاث من Firestore حتمًا.

---

# عمليات مشروطة يمكن تأجيلها

1. **primePhoneCounterBase** (بند 1 أعلاه): يكفي تنفيذها عند أول نقرة "حفظ" في صفحات إضافة الهواتف — لا لزوم لها في 13 صفحة أخرى.
2. **view_sale.html:1060** (phones للطباعة): تؤجل إلى نقرة زر الطباعة (هي كذلك أصلًا، لكن قراءة العرض :674 نفسها مؤجلة-قابلة: لا تحتاج إلا عند وجود عناصر هاتف ناقصة serial_number).
3. **view_sale.html:1246** (accessories عند الإرجاع): منفذة عند النقر فعلًا — نمط صحيح؛ المطلوب فقط استبدال القراءة الكاملة بـ getDoc لكل عنصر.
4. **create_sale.html:291** (sales كاملة عند فتح الصفحة): لا تلزم إلا لاستبعاد الهواتف المباعة من القائمة المنسدلة؛ يمكن جلبها عند اختيار نوع المنتج "هاتف" فقط.
5. **list_accessories_simple.html exportData** (إن أُعيد تفعيل الزر): قراءات التصدير يجب أن تبقى عند النقر فقط (حاليًا لا زر أصلًا).
6. **maintenance-jobs-report.html:399-400** (reps/technicians): تلزم فقط عند وجود repId/techId في الرابط.

---

# تحقق بصري مطلوب

1. **view_sale.html — خانة الرقم التسلسلي في جدول الأصناف**: هل توجد فواتير فعلية عناصرها بلا `serial_number` (فيضطر الكود لجلب كل الهواتف)؟ إن كانت كل الفواتير تحفظ الرقم داخل الفاتورة، تكون قراءة phones الكاملة بلا فائدة مطلقة. اختبر فاتورة فيها هاتف.
2. **maintenance-jobs-report.html — شريط "الفلاتر المطبقة"**: تأكد بصريًا من ظهور اسم المندوب/الفني عند الفتح من الروابط العادية (بدون معاملات) — إن كان الشريط يظهر دائمًا فارغًا فهذا يؤكد أن قراءتي reps/technicians بلا فائدة في أغلب الاستخدام.
3. **add_new_phone.html / add_used_phone.html (وضع ?edit=)**: مثبت من الكود أن `loadPhoneForEdit` يكتب في عناصر غير موجودة: `#color` و`#memory` و`#serial` (grep: صفر نتائج لهذه المعرّفات، والصحيح `phone_color/phone_memory/serial_number`) — `document.getElementById('color').value` عند :555 يرمي TypeError على null فيتوقف ملء بقية الحقول (الذاكرة/التسلسل/الأسعار). المطلوب بصريًا: تأكيد أن فتح رابط تعديل هاتف يظهر نموذجًا ناقص الحقول فعلًا، وأن القراءة الكاملة للهواتف تُستهلك جزئيًا فقط.
4. **edit_accessory.html**: هل يوجد أي مسار وصول لهذه الصفحة من الواجهة؟ إن لم توجد، الصفحة كلها (بيانات وهمية + استدعاء مكسور) مرشحة للإزالة.
5. **limited_dashboard.html**: تأكد من عدم وجود أي بطاقات إحصاء متوقعة من المستخدمين (الدالة فارغة عمدًا بتعليق "لم تعد هناك إحصائيات معروضة").

---

# تقدير الأثر

بافتراض: N = عدد مستندات phones، A = accessories، S = sales، C = accessory_categories، PT = phone_types، J = maintenanceJobs، R = reps، T = technicians، P = payments.

- **فتح dashboard.html** = 1 (عداد) + N + A + S + J₍done₎. لـ 10 فتحات يوميًا = 10 × (1 + N + A + S + J₍done₎).
- **فتح inventory_summary.html** = 1 (عداد) + N + A + S — منها S مهدورة (بند 3). لـ 10 فتحات = 10 × (1 + N + A + S)، القابل للتوفير 10 × S.
- **كل عملية بيع** (فتح create_sale + حفظ + view_sale) = (1 + N + A + C + S) + مهدر إعادة التحميل (N + A + C + S) + 1 (getSale) + N = تكلفة قابلة للتوفير تبلغ N + A + C + S لكل عملية بيع.
- **كل عملية بحث** في search.html = 1 (عداد) + N + A + S، منها S لشارة "مباع" فقط.
- **عرض فاتورة + طباعتها** = 1 (عداد) + 1 (مستند) + N + N = توفير محتمل 2N − (عدد عناصر الهاتف).
- **فتح maintenance.html** = 2 × J (تكرار مؤكد).
- **فتح maintenance-accounts.html** = 2 × J₍done₎ + P (تكرار مؤكد).
- **فتح maintenance-jobs-report.html** = J₍مفلتر₎ + R + T (أغلب R+T مهدور).
- **إجمالي الضريبة الثابتة**: قراءة عداد واحدة لكل فتح لأي صفحة من الصفحات الـ 15 التي تحمّل storage-manager — إن تنقّل المستخدم بين 10 صفحات في جلسة = 10 قراءات لا تخدم شيئًا.

مثال رقمي توضيحي (S=500 عملية بيع): التوفير من البنود 2 + 3 + 4 وحدها (كل منها يوفر قراءة مجموعة sales كاملة) في يوم فيه 10 عمليات بيع و10 عمليات بحث و10 فتحات لملخص المخزون = 10×S + 10×S + 10×S = 30×S = **15,000 قراءة مستند يوميًا** (≈450,000 شهريًا) قابلة للتوفير من ثلاثة بنود فقط، دون احتساب وفورات الهواتف والأكسسوارات في البند 2.

---

# عناصر لا يجب حذفها

1. **dashboard.html — قراءات phones/accessories/sales الكاملة** (وعددها كبير): تبدو مكلفة لكنها كلها تُستخدم فعلًا في بطاقات الإحصاء والمجاميع وجدول آخر 5 مبيعات (البقية تدخل في حسابات المجاميع، ليست مهملة). لا تحذفها — يمكن فقط استبدالها لاحقًا بحسابات مجمّعة côté الخادم.
2. **view_sale.html:585 — `getSale(saleId)`**: قراءة مستند واحد هي النمط الصحيح؛ لا تستبدلها بقراءة مجموعة.
3. **maintenance-invoice.html:407 — `getMaintenanceJob(jobId)`**: مستند واحد بالمعرّف — نمط exemplary، كل بياناته معروضة في الفاتورة.
4. **list_sales.html:368 — قراءة sales الكاملة**: ضرورية للجدول والمجاميع والترقيم؛ الفلاتر تعمل على الذاكرة دون إعادة جلب (سلوك exemplary).
5. **getNextPhoneNumber في صفحات إضافة الهواتف** (setDoc increment + getDoc على counters/phones): ليست تكرارًا عبثيًا — هي ضمان تفرد رقم الباركود. فحص `hasPhoneWithNumber` الإضافي داخل `addPhone` (cdn:260) يبدو زائدًا بعد العداد الذري، لكنه شبكة أمان ضد انحراف العداد — لا يُحذف إلا مع بديل مثبت.
6. **getMaintenanceJob الإضافي داخل updateMaintenanceJob** (cdn:924) عند كل حفظ تعديل: قراءة مستند واحد لإعادة حساب الأرباح المشتقة — قد تبدو زائدة لأن الصفحة تحسبها محليًا، لكنها تضمن صحة القيم عند التعديل الجزئي للحقول؛ إزالتها تتطلب إثباتًا أن الاستدعاء يمرر دائمًا كل الحقول.
7. **مزامنة `syncPhoneCounterToMax`** (cdn:221 — قراءة phones كاملة لحساب max): تنفذ فقط عند اكتشاف تكرار باركود (مسار خطأ نادر)، ليست تحميلًا روتينيًا — لا تحذفها.
8. **الكاش المزدوج في dashboard.html** (5 دقائق فوق كاش الدقيقتين): يبدو تكرارًا لكنه يقلل القراءات فعلًا؛ المشكلة الوحيدة احتمال تضارب انتهاء الصلاحيتين.
9. **`loadData()` الكاملة بعد الحفظ في maintenance-jobs.html:1027 وmaintenance-reps/maintenance-technicians بعد كل CRUD**: إعادة تحميل مجموعة لتحديث جدول مرئي — ليست هدرًا (الجدول يُعاد عرضه فعليًا)، وإن كان يمكن تحسينها محليًا.
10. **قراءة الفلاتر في search.html/loadAllData**: بيانات الجداول تُعرض كلها — التوفير الوحيد فيها هو شريط sales (بند 4) وليس phones/accessories.

---

## خلاصة الأولويات

أعلى توفير بأقل جهد، بالترتيب:
1. تأجيل `primePhoneCounterBase` (بند 1) — 1 قراءة لكل فتح صفحة، تعديل سطر واحد.
2. إزالة `loadDataFromFirebase()` بعد إتمام البيع (بند 2) — 4 مجموعات لكل عملية بيع.
3. توحيد قراءات maintenanceJobs المكررة (بنود 6، 7).
4. استبدال نمط "قراءة مجموعة كاملة لإيجاد سجل واحد" (بند 10) بـ where-query/getDoc.
5. معالجة sales الكاملة لغرض isPhoneSold (بنود 3، 4) بعلم `sold` على مستند الهاتف.
